# node-red-contrib-llama-cpp
A Node-RED node that runs llama-server (llama.cpp) as a child process and exposes on-device LLM inference directly in your flows — no cloud, no Docker, no external service required. Designed for edge devices and SBCs (Raspberry Pi, Radxa, Orange Pi…), but works on any machine where llama.cpp runs.
